# Browser calls the API only through the same-origin /api prefix (Vite proxy in
# dev, Vercel rewrite in prod) so the session cookie stays first-party.
# Leave as /api in dev; the Vite proxy forwards /api/* to http://localhost:3000.
VITE_API_BASE_URL=/api
